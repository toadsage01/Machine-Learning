# P6_leaf_disease dependencies (pinned)
# Python >= 3.10
#
# Core numerics
numpy==2.1.3
scipy==1.14.1
pandas==2.2.3
requests==2.32.3
Pillow==11.3.0

# Deep learning — CPU-only torch build (~190 MB instead of ~2 GB CUDA build).
# To enable GPU, replace with the cuda wheels from
# https://download.pytorch.org/whl/cu121
torch==2.13.0
torchvision==0.28.0

# Augmentations
albumentations==2.0.8
opencv-python-headless==4.13.0

# ONNX export & runtime
onnx==1.22.0
onnxruntime==1.28.0

# TFLite export & runtime (Google's ai-edge-litert is the maintained fork
# of the deprecated tflite-runtime package).
ai-edge-litert==2.2.0

# Plotting (uses shared/plot_style.mplstyle via the repo-root package)
matplotlib==3.9.2
